<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.robin.mapper.StudentMapper">
	<resultMap type="com.robin.pojo.Student" id="resultMap1">
		<result column="SEX" property="sex"
			typeHandler="org.apache.ibatis.type.EnumOrdinalTypeHandler" />
	</resultMap>
	<select id="getAllStudentsByList" resultMap="resultMap1">
		select
		STD_ID,NAME,BIRTHDAY,SEX from STUDENT
	</select>

	<!-- 遗憾的是，这种配置方式我并没有找到合适的办法 -->
	<!-- <select id="getAllStudentsByMapStudent" resultMap="resultMap2"> -->
	<!-- select -->
	<!-- STD_ID,NAME,BIRTHDAY,SEX from STUDENT -->
	<!-- </select> -->

	<select id="getAllStudentsByArr" resultMap="resultMap1">
		select
		STD_ID,NAME,BIRTHDAY,SEX from STUDENT
	</select>

	<!-- <resultMap type="java.util.HashMap" id="resultMap4"> -->
	<!-- </resultMap> -->
	<select id="getAllStudentsByMapList" resultType="map">
		select
		STD_ID,NAME,BIRTHDAY,SEX from STUDENT
	</select>

</mapper>